Int x;
Int y;
Int z;

Bool function isPositive (Int n) {
    n > 0;
}

Int function sum (Int a, Int b) {
    if (a > b) {
        a + b;
    } else {
        b + a + 1;
    }
}

forall(Int x) {
    isPositive(x) -> (sum(x, 2) > 5);
}

exists(Int y) {
    y > 0 and sum(y, 3) < 10;
}

check;

print x;
print y;
